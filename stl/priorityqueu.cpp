#include<iostream>
#include<queue>

using namespace std;
int main(){

	prioiry_queue<int> pq;
	for(int i=1;i<n;i++)
	{
		int no;
		cin>>no;
		pq.push(no);

		
	}
	//remove elementv from the heap 
	while(!pq.empty(){
		cout<<pq.top()<<",";
		pq.pop();
	}

		
}
//priority telling highe num has high priority , actually implemented as a heap  
//push -(logn)
//pop-(logn)
//top 
//max heap by default
