stack =[]
stack.append(10)
	stack.append(20)
	print (Stack)
	print(stack.pop)
	 
	top=statck[-1]
	print(top)
	size=len(stack)
	print(size)
strcut mystack{\
	int *arr;
	int cap;
	int top;
	mystack(int c){
		cap=c;
		
		arr=new int[cap]
			top=-1;
	void push(int x)

	if(top==cap-1)
	top++
	arr[top]=x

	int pop
	if(top==-1)
		int res=arr[top];
		top--;
		return res;
	
	int peek()
		if(top==-1)
			count--stack empty
				retunr arr[top]
				int size()top
